// [id] is a dynamic segment and the page depends on the variable (game id).
// all game specific screen (play) live under the same game id, so you always have params.id handy and a single place to fetch the doc
